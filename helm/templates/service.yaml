apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventaris.fullname" . }}-user
  labels:
    app: user-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3001
      protocol: TCP
  selector:
    app: user-service
