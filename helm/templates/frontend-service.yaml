apiVersion: v1
kind: Service
metadata:
  name: {{ include "fullname" . }}-frontend
  labels:
    app: frontend
spec:
  type: {{ .Values.services.user.type }}
  ports:
    - port: {{ .Values.services.user.port }}
      targetPort: {{ .Values.services.user.port }}

